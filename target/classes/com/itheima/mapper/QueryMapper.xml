<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.QueryMapper">
    <select id="teacherUsernameQuery" resultType="String">
        select username
        from oa_sys.teacher
        where username = #{username}
    </select>
    <select id="departmentUsernameQuery" resultType="String">
        select username
        from oa_sys.department
        where username = #{username}
    </select>
    <select id="leaderUsernameQuery" resultType="String">
        select username
        from oa_sys.leader
        where username = #{username}
    </select>
    <select id="departmentMessage" resultType="Department">
        select uid, realeName
        from oa_sys.department
        where status = 0
    </select>
    <select id="leaderMessage" resultType="Leader">
        select uid, realeName
        from oa_sys.leader
        where status = 0
    </select>
</mapper>