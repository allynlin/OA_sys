<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cshbxy.mapper.ProcessMapper">
    <select id="processQueryByUid" resultType="Process">
        select uid, name, process, create_time, update_time
        from process
        where uid = #{uid}
    </select>
    <select id="processQueryByName" resultType="Process">
        select uid, name, process, create_time, update_time
        from process
        where name = #{name}
    </select>
    <update id="processUpdate" parameterType="Process">
        update process
        set name=#{name},
            process=#{process},
            update_time=now()
        where uid = #{uid}
    </update>
    <insert id="processInsert" parameterType="Process">
        insert into process(uid, name, process)
        values (#{uid}, #{name}, #{process})
    </insert>
    <delete id="processDelete">
        delete
        from process
        where uid = #{uid}
    </delete>
</mapper>