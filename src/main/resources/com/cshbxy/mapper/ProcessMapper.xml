<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cshbxy.mapper.ProcessMapper">
    <select id="processQueryByUid" resultType="Process">
        select uid, name, process, Alias, status, create_time, update_time
        from oa_sys.process
        where uid = #{uid}
    </select>
    <select id="processQueryByName" resultType="Process">
        select uid, name, process, Alias, create_time, update_time
        from oa_sys.process
        where name = #{name}
          and status = 0
    </select>
    <update id="processUpdate" parameterType="Process">
        update oa_sys.process
        set name=#{name},
            process=#{process}
        where uid = #{uid}
    </update>
    <insert id="processInsert" parameterType="Process">
        insert into oa_sys.process(uid, name, process)
        values (#{uid}, #{name}, #{process})
    </insert>
    <delete id="processDelete">
        delete
        from oa_sys.process
        where uid = #{uid}
    </delete>
</mapper>